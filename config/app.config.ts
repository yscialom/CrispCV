// config/app.config.ts
export const APP_CONFIG = {
  appTitle: 'CrispCV - Online Resume', // Application's HTML page title
  defaultTheme: 'dark', // Default theme for the application ('light' or 'dark')
  defaultLanguage: 'en_US', // Default language if no browser/stored preference found
};
